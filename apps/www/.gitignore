# specific to this app
/.clerk/

# local env files (can opt-in for committing if needed)
# .env* is covered by root only for .env*.local. 
# Web had .env* which ignores .env.production etc.
# Root .gitignore has: .env*.local and .env
# If we want to maintain the same behavior as before (ignoring everything except .example):
.env*
!.env.example
