/**
 * Page objects for every app, shared by every suite that drives them. They
 * describe the apps, not the tests: keep assertions in the specs, so a
 * marketing capture can walk the same pages without inheriting a test's
 * expectations.
 */

export { HomePage } from "./home.page";
export { LandingPage } from "./landing.page";
